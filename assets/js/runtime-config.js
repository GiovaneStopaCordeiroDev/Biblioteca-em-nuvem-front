// Este arquivo não deve conter segredos. Ajuste apiBaseUrl no artefato publicado
// quando o front-end e a API estiverem hospedados em origens diferentes.
export const runtimeConfig = Object.freeze({
  apiBaseUrl: "https://biblioteca-em-nuvem.onrender.com/api/v1",
  requestTimeoutMs: 15_000,
});